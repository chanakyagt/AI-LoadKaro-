/** Shared copy for paginated list screens (filters + pager). */
export const LIST_SCREEN_LABELS = {
  next: 'Next',
  previous: 'Previous',
  page: 'Page',
  apply_filter: 'Apply filter',
  clear_filter: 'Clear filter',
  destination_optional:
    'Destination is optional. Leave it blank to filter by origin only.',
} as const;
